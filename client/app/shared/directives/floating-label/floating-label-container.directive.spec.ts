import { FloatingLabelContainerDirective } from './floating-label-container.directive';

describe('shared.directives:FloatingLabelContainerDirective', () => {
  it('should create an instance', () => {
    // const directive = new FloatingLabelContainerDirective(host, renderer);
    // expect(directive).toBeTruthy();
  });
});
