describe('ClubController', () => {
  beforeEach(() => {
  });

  it('can retreive all clubs', () => {
  });

  it('can retreive a single club by id', () => {
  });

  it('can create a club', () => {
  });

  it('can update a club', () => {
  });

  it('can delete a club', () => {
  });

  it('can retreive a clubs members', () => {
  });

  it('can create members in a club', () => {
  });

  it('can remove members from a club', () => {
  });
});
